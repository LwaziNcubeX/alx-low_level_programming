#include <stdio.h>
/**
 * main - Entry point
 *
 * Return - 0 meaning success
 */
int main(void)
{
	char Letter;
	for (Letter = 'a'; Letter <= 'z'; Letter++)
		putchar(Letter);
	for (Letter = 'A'; Letter <= 'Z'; Letter++)
		putchar(letter)

	putchar('\n');
	return (0);
}
