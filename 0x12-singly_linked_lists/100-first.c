#include "lists.h"
#include <stdio.h>

/**
 * print_first - entry point
 *
 * Return: void
 */
void print_first(void)
{
	char a[] = "You're beat! and yet, you must allow,";

	printf("%s\nI bore my house upon my back!\n", a);
}

/**
 * main - check the code
 *
 * Return: Always 0.
 */
int main(void)
{
	char b[] = "A tortoise, having pretty good sense of";

	printf("(%s a hare's nature, challenges one to a race.)\n", b);

	return (0);
}
