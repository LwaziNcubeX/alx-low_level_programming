#include <stdio.h>
#include "main.h"
/**
 * print_alphabet - Entry point
 *
 * Return: Always 0
 */

void print_alphabet(void);
{
	char letter = 'z'

	while (letter >= 'a'; letter++)
	{
		putchar(letter);
	}

	putchar('\n');
}
