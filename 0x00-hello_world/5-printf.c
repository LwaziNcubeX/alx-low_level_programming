#include <stdio.h>
/**
 * main - prints "with proper grammar, but the outcome is a piece of art,"
 *
 * Return: always 0 unless stated otherwise...
 */
int main(void)
{	char str_3[] = "with proper grammar, but the outcome is a piece of art,";

	printf("%c", *str_3);
	return (0);
}
