#include "main.h"
/**
 * mul - entry point + multiplication os 2 values
 * @a: assigned numer
 * @b: assigned number
 * Return: prodof (success)
 * prodof - meaning product of
 */
int mul(int a, int b)
{
	int prodof = 0;

	prodof = a * b;
	return (prodof);
}
